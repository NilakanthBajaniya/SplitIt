function RetrieveTabValues() {

    var monthlyIncome = 0, monthlyExpense = 0,
        totalIncome = 0, totalExpense = 0;

    var incomes = Utility.getKeyData("income");
    var expenses = Utility.getKeyData("expense");

    $.each(incomes, function (index, value) {

    });

}

